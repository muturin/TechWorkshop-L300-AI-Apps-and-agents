name: Deploy Interior Designer Agent

on:
  push:
    branches: [main]
    paths:
      - 'src/infra/agents/interior-designer.json'
      - 'src/prompts/InteriorDesignAgentPrompt.txt'
      - 'src/app/tools/aiSearchTools.py'
      - 'src/app/agents/interiorDesignAgent_initializer.py'
  workflow_dispatch:

jobs:
  deploy-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2.1.1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy Interior Designer agent to Microsoft Foundry
        env:
          FOUNDRY_ENDPOINT: ${{ secrets.FOUNDRY_ENDPOINT }}
          GPT_DEPLOYMENT: ${{ secrets.GPT_DEPLOYMENT }}
        run: |
          PAYLOAD=$(jq --arg model "$GPT_DEPLOYMENT" \
            --arg instructions "$(cat src/prompts/InteriorDesignAgentPrompt.txt)" \
            '.definition.model = $model | .definition.instructions = $instructions' \
            src/infra/agents/interior-designer.json)

          az rest --method POST \
            --url "${FOUNDRY_ENDPOINT}/agents/interior-designer/versions?api-version=2025-11-15-preview" \
            --resource "https://ai.azure.com" \
            --body "$PAYLOAD" \
            --headers "Content-Type=application/json"

          echo "Interior Designer agent deployed successfully."
